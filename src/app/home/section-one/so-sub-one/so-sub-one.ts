import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
	selector: 'so-sub-one',
	imports: [RouterLink],
	templateUrl: './so-sub-one.html'
})

export class SOSubOne {

}
